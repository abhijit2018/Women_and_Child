# Copy this file to .env and fill in real values.
# Never commit the actual .env file — it's already git-ignored.

# Base URL of the backend API this frontend talks to.
VITE_API_BASE_URL=http://localhost:5000/api
